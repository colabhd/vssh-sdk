{
  "id": "print-engine",
  "name": "Motor de Impressão",
  "version": "4.0.0",
  "type": "engine",
  "kind": "service",
  "category": "Engine",
  "description": "Gera PDF no próprio servidor, com o mesmo motor de renderização do navegador — então o PDF se parece com o que a pessoa viu na tela. Sem janela própria: é consumido pelo diálogo de impressão do ambiente.",
  "provides": ["print/v1"],
  "requiredPackages": ["chromium | chromium-browser | google-chrome-stable | microsoft-edge-stable"],
  "resources": {
    "memoryMax": "1G",
    "tasksMax": "256"
  },
  "minShellVersion": "4.1.0",
  "backend": {
    "runtime": "node",
    "transport": "socket",
    "entrypoint": "backend/server.js",
    "installCommand": "( [ \"${VSSH_APP_REBUILD:-}\" != 1 ] && test -d node_modules ) || npm ci --omit=dev",
    "healthcheckPath": "/healthz"
  }
}
